export const pagesSizes = [5, 10, 30, 50, 100];
export const pageSizeDefault = pagesSizes[1];
export const pageDefault = 1;

// Tailles adaptées aux vues cartes (grilles), plus denses que les lignes d'une table.
export const pagesSizesForCard = [6, 9, 12];
