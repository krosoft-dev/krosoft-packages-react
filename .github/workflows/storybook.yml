name: Storybook

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Le déploiement de design.krosoft.fr lance ce même build : une story
  # cassée doit échouer ici, pas dans le pipeline Azure DevOps.
  storybook:
    uses: krosoft-dev/Krosoft.DevOps.GitHub/.github/workflows/npm-storybook.yml@main
